<div>
  <h1>PERFIL DEL USUARIO</h1>
  <p>{{firstname}} {{lastname}}</p>
  <p>Edad: {{age}}</p>
  <p>Email: {{email}}</p>
  <p>ROL: {{role}}</p>
  <div>
    <form action="/api/user/logout" method="get">
      <br />
      <button type="submit">Cerrar sesión</button>
    </form>
  </div>
  <div>
    {{#if isAdmin}}
    <a href="/products">Ir a administrar los productos</a>
    {{else}}
      {{#if isPremium}}
      <a href="/products">Ir a administrar los productos</a>
      <div>
        <a href="/chat">Ir a enviar mensajes al chat</a> <br>
        <a href="/cartsuser">Ir a ver mi carrito</a>
      </div>
      {{else}}
      <div>
        <a href="/chat">Ir a enviar mensajes al chat</a> <br>
        <a href="/cartsuser">Ir a ver mi carrito</a>
      </div>
      <h1>{{firstName}} {{lastName}}</h1>
      {{/if}}
    {{/if}}
  </div>
</div>